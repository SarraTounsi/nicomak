<form method="post" action="{{ path('app_merci_delete', {'id': merci.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ merci.id) }}">
    <button class="btn">Delete</button>
</form>
