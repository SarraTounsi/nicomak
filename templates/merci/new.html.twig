{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/new.css') }}">
{% endblock %}
{% block title %}Nouveau Merci{% endblock %}

{% block body %}
<h1>Dire Merci</h1>

{{ form_start(form) }}
    {{ form_row(form.fromEmployee) }}
    {{ form_row(form.toEmployee) }}
    {{ form_row(form.message) }}
    {{ form_row(form.date) }}
    <button class="btn">Envoyer</button>
{{ form_end(form) }}

<a href="{{ path('app_merci_index') }}">Retour à la liste</a>
{% endblock %}
